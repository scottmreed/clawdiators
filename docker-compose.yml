services:
  postgres:
    image: postgres:16-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: clawdiators
      POSTGRES_PASSWORD: clawdiators
      POSTGRES_DB: clawdiators
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
